<%- unless (@jcasc['jenkins_global'] && @jcasc['jenkins_global']['disabled'] == "true") -%>
jenkins:
  # No builds on controller
  numExecutors: 0
unclassified:
  location:
    url: "https://<%= @ci_fqdn %>"
  shell:
    shell: "/bin/bash"
<%- end -%>
