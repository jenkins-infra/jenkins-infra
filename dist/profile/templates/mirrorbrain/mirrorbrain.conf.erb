[general]
instances = main

[main]
dbuser = <%= @pg_username %>
dbpass = <%= @pg_password %>
dbdriver = postgresql
dbhost = <%= @pg_host %>
dbname = <%= @pg_database %>

[mirrorprobe]
# logfile = /var/log/mirrorbrain/mirrorprobe.log
# loglevel = INFO
