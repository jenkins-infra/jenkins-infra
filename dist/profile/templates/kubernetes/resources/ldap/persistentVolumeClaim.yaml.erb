kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: backup
  namespace: ldap
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  selector:
      matchLabels:
          app: ldap
