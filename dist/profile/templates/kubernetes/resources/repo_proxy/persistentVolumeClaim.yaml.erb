kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: repo-proxy
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  selector:
      matchLabels:
          app: repo-proxy
