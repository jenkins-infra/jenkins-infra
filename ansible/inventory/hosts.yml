---
all:
  children:
    archives:
      hosts:
        archives.do.jenkins.io:
          ansible_host: archives.do.jenkins.io
          ansible_user: ubuntu
          ansible_ssh_private_key_file: ~/.ssh/jenkins_infra_key
          # Host-specific variables are in host_vars/archives.do.jenkins.io.yml
          # Override variables per host if needed
          # rsync_hosts_allow:
          #   - "0.0.0.0/0"
          #   - "::/0"
          # ssh_authorized_keys:
          #   - key: "ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAABAQ..."
          #     comment: "deploy key"
